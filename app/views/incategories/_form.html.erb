<%= form_with(model: incategory, local: true) do |form| %>
  <% if incategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(incategory.errors.count, "error") %> prohibited this incategory from being saved:</h2>

      <ul>
      <% incategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titre %>
    <%= form.text_field :titre %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
