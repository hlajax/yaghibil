<%= form_with(model: fond, local: true) do |form| %>
  <% if fond.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fond.errors.count, "error") %> prohibited this fond from being saved:</h2>

      <ul>
      <% fond.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titre %>
    <%= form.text_field :titre %>
  </div>

  <div class="field">
    <%= form.label :montant %>
    <%= form.number_field :montant %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
