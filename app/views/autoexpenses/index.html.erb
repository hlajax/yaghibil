        <%=render 'menus/top'%>
        <div id="layoutSidenav">
            <%=render 'menus/side'%>
                         <div id="layoutSidenav_content">
                <main>
                    <div class="container-fluid">
                        <h1 class="mt-4">Dépenses</h1>
                        <div class="row">
                          <div class="col-lg-12 col-12">
<p id="notice"><%= notice %></p>
  <div class="card bg-danger text-white mb-4">
                                    <div class="card-body">
                                      <div>
  <h3>Total de dépenses du mois: <b><%= Autoexpense.total_for_current_month %> XFA</b>.</h3>
</div>

                                    </div>
                                    <div class="card-footer d-flex align-items-center justify-content-between">
                                        <%= link_to "Bilan", statistics_path, class:"small text-white stretched-link" %>
                                        <div class="small text-white"><i class="fas fa-angle-right"></i></div>
                                    </div>
                                </div>
                                
<%= link_to 'Nouvelle Dépense', new_autoexpense_path, class:"btn btn-danger" %>
<div class="table-responsive mt-2">
<table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
  <thead>
    <tr class="bg-dark text-white">
      <th>Prix</th>
      <th>Description</th>
      <th>Travaux</th>
      <th>Véhicule</th>
      <th>Dépensé le</th>
      <th colspan="3" class="text-center">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @autoexpenses.each do |autoexpense| %>
      <tr>
        <td><%= autoexpense.price %></td>
        <td><%= autoexpense.description %></td>
        <td><%= autoexpense.travaux %></td>
        <td><%= autoexpense.automobile.marque %> <%= autoexpense.automobile.modele %></td>
        <td><%= autoexpense.billed_at %></td>
        <td><%= link_to 'Voir', autoexpense %></td>
        <td><%= link_to 'Modifier', edit_autoexpense_path(autoexpense) %></td>
        <td><%= link_to 'Supprimer', autoexpense, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>



</div>
                        </div>
                    </div>
                </main>
               <footer class="py-4 bg-light mt-auto">
                    <div class="container-fluid">
                        <div class="d-flex align-items-center justify-content-between small">
                            <div class="text-muted">Copyright &copy; <b>YAGHIBIL</b> 2020</div>
                         
                        </div>
                    </div>
                </footer>
            </div>
           
        </div>