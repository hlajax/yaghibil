        <%=render 'menus/top'%>
        <div id="layoutSidenav">
            <%=render 'menus/side'%>
                         <div id="layoutSidenav_content">
                <main>
                    <div class="container-fluid">
                        <h1 class="mt-4">Dépenses</h1>
                        <div class="row">
                          <div class="col-lg-12 col-12">
                            <p id="notice" class="text-success"><%= notice %></p>
                            <div class="card bg-primary text-white mb-4">
                                    <div class="card-body">
                                      <div>
  <h3>Total de dépenses du mois: <b><%= Expense.total_for_current_month %> XFA</b>.</h3>
</div>

                                    </div>
                                    <div class="card-footer d-flex align-items-center justify-content-between">
                                        <%= link_to "Bilan", statistics_path, class:"small text-white stretched-link" %>
                                        <div class="small text-white"><i class="fas fa-angle-right"></i></div>
                                    </div>
                                </div>

<%= link_to 'Ajouter une dépense', new_expense_path, class:"btn btn-primary mt-2" %>

<div class="table-responsive mt-2">
  <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
  <thead>
    <tr class="bg-dark text-white">
      <th>Titre</th>
      <th>Montant</th>
      <th>Catégorie</th>
      <th>Source</th>
      <th>Dépensé le</th>
      <th colspan="3" class="text-center">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @expenses.each do |expense| %>
      <tr>
        <td><%= expense.titre %></td>
        <td><%= expense.price %> XFA</td>
        <td><%= expense.category.titre%></td>
        <td><%= expense.source.titre %></td>
        <td><%= expense.billed_at %></td>
        <td><%= link_to 'Voir', expense %></td>
        <td><%= link_to 'Modifier', edit_expense_path(expense) %></td>
        <td><%= link_to 'Supprimer', expense, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
                          </div>
                        </div>
                    </div>
                </main>
               <footer class="py-4 bg-light mt-auto">
                    <div class="container-fluid">
                        <div class="d-flex align-items-center justify-content-between small">
                            <div class="text-muted">Copyright &copy; <b>YAGHIBIL</b> 2020</div>
                         
                        </div>
                    </div>
                </footer>
            </div>
           
        </div>