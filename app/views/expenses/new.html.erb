 <%=render 'menus/top'%>
        <div id="layoutSidenav">
            <%=render 'menus/side'%>
                         <div id="layoutSidenav_content">
                <main>
                    <div class="container-fluid">
<h1>Ajouter une dépense</h1>

<%= render 'form', expense: @expense %>

<%= link_to 'Retour', expenses_path, class:"btn btn-danger" %>
</div>
</main>
</div>
</div>